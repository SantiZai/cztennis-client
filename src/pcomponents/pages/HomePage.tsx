import { ModeToggle } from "@/components/mode-toggle";
import "../../app.css";

const HomePage = () => {
    return (
        <div className="container-home w-full h-full">
            <ModeToggle />
        </div>
    );
};

export default HomePage;
